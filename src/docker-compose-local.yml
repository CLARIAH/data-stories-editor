services:
  stories:
    image: data_stories
    build:
      context: .
    environment:
      - APP_DOMAIN=http://localhost
    volumes:
      - /Users/robzeeman/Documents/DI_code/data_stories/localhost.data:/app/service/data
    ports:
      - 80:80

